From: ../features/steps/behave_test.py


From: ../features/steps/behave_test.py



**we have behave installed**
   If behave is not installed we couldnt get here!

**we implement a test**
   A trivial assertion.

**behave will test it for us!**
   The trivial assertion did not fail.

**we have behave installed**
   If behave is not installed we couldnt get here!

**we implement a test**
   A trivial assertion.

**behave will test it for us!**
   The trivial assertion did not fail.

**we have behave installed**
   If behave is not installed we couldnt get here!

**we implement a test**
   A trivial assertion.

**behave will test it for us!**
   The trivial assertion did not fail.
